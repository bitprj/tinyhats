apiVersion: apps/v1
kind: Deployment
metadata:
  name: upload-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: upload-service
  template:
    metadata:
      labels:
        app: upload-service
    spec:
      containers:
        - name: upload-service
          image: bitprj/upload-service
          ports:
            - containerPort: 8080
          env:
            - name: HOST
              value: babieswithhats.coqtasittblb.us-east-2.rds.amazonaws.com
            - name: PASSWORD
              value: x7CRPKg&t&^F
            - name: S3_ID
              value: AKIA5N7DT3EGUDGBFMIH
            - name: S3_SECRET
              value: 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
          imagePullPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: upload-service
spec:
  selector:
    app: upload-service
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer