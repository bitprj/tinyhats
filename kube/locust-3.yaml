apiVersion: apps/v1
kind: Deployment
metadata:
  name: locust-service-3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: locust-service-3
  template:
    metadata:
      labels:
        app: locust-service-3
    spec:
      containers:
        - name: locust-service
          image: bitprj/locust-service-3
          ports:
          - containerPort: 5557
            name: comm
          - containerPort: 5558
            name: comm-plus-1
          - containerPort: 8089
            name: web-ui
          env:
            - name: ATTACKED_HOST
              value: http://frontend-service:80
          imagePullPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: locust-service-3
spec:
  selector:
    app: locust-service-3
  ports:
    - port: 80
      targetPort: 8089
  type: ClusterIP
