apiVersion: apps/v1
kind: Deployment
metadata:
  name: locust-service-1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: locust-service-1
  template:
    metadata:
      labels:
        app: locust-service-1
    spec:
      containers:
        - name: locust-service
          image: bitprj/locust-service-1
          ports:
          - containerPort: 5557
            name: comm
          - containerPort: 5558
            name: comm-plus-1
          - containerPort: 8089
            name: web-ui
          env:
            - name: ATTACKED_HOST
              value: http://gateway-service:80
          imagePullPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: locust-service-1
spec:
  selector:
    app: locust-service-1
  ports:
    - port: 80
      targetPort: 8089
  type: ClusterIP
